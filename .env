
# Variable passing chain for build and runtime:
# 1. .env:                        Defines all initial values
#    - SDK_INSTALL_DIR=/opt/rk3588s
#    - SDK_PKG_NAME=rk3588s-linux_20240913.tar.xz
#    - CONTAINER_VOLUME_ROOT=/host_disk
#
# 2. docker-compose.yml:          Passes values to Dockerfile via build args
#    build:
#      args:
#        SDK_INSTALL_DIR: ${SDK_INSTALL_DIR}
#        SDK_PKG_NAME: ${SDK_PKG_NAME}
#        CONTAINER_VOLUME_ROOT: ${CONTAINER_VOLUME_ROOT}
#
# 3. ARG:                        Receives values during build time
#    - Only available during image build
#    - Not persisted in final image
#    - Must be declared before ENV if used in ENV
#
# 4. ENV:                        Makes variables available at runtime
#    - Persists in final image
#    - Available to all subsequent build steps
#    - Available to running containers
#
PROJECT_NAME=rk3588s-n8 # Docker container name
TAG=1.0 # Docker image tag
DOCKER_BUILDKIT=1 # Enable Docker BuildKit for improved build performance

SSH_MOUNT=/c/Users/MrJames/.ssh # SSH key mount point in Host

SDK_PKG_NAME=rk3588s-linux_20240913.tar.xz # SDK package name
CONTAINER_VOLUME_ROOT=/docker_volume # Docker volume mount point in container
SDK_INSTALL_DIR=/sdk_development # the directory to install SDK in container