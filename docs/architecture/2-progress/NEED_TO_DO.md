- **改完就把下面的checkbox checked**

Mar26.2026 23:00
- [x] Fix README.md: all `doc/` references need updating to `docs/` after directory rename
- [ ] Evaluate: should `docker/libs/iv_scripts/setup_base.sh` be deleted or merged into stage_1 version?
- [ ] Evaluate: should `docker/libs/ii_dockerfile_modules/*.df` be deleted (never used by current Dockerfile)?
- [ ] `docker-compose.yaml` in project_handover still has hardcoded values — should use `${VAR}` from `.env`
