# =============================================================================
# OS Packages — ubuntu/20.04.env
# Ubuntu 20.04-only packages (unavailable or different on newer releases).
# Total: 5 packages
# =============================================================================

# ── 1. Python 2.7 (removed from Ubuntu 22.04+) ────────────────────────────
python2.7
python2.7-dev
libpython2.7
libpython2.7-dev

# ── 2. ICU (required by rv1126bp SDK build process) ───────────────────────
libicu-dev
