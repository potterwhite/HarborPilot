# =============================================================================
# OS Packages — ubuntu/24.04.env
# Ubuntu 24.04-specific packages.
# Total: 2 packages
# =============================================================================

# ── 1. Android repo tool (not available on 20.04) ─────────────────────────
repo

# ── 2. ncurses (libncurses-dev is the default on 22.04+) ──────────────────
# On 20.04, libncurses5-dev is required instead (see 20.04.env).
libncurses-dev