# =============================================================================
# OS Packages — ubuntu/common.env
# Ubuntu-family packages available on all Ubuntu versions.
# Total: 12 packages
# =============================================================================

# ── 1. Build tools ────────────────────────────────────────────────────────
# cmake is controlled by INSTALL_HOST_CMAKE in host config.
cmake

# ── 2. ncurses (Ubuntu default: libncurses-dev, transitional on 20.04) ───
libncurses-dev

# ── 3. Shell and CLI UX ───────────────────────────────────────────────────
# bash-completion is controlled by INSTALL_VCS_TOOLS in host config.
bash-completion

# ── 4. Man pages and documentation ────────────────────────────────────────
# Controlled by INSTALL_MAN_DOC flag in host config.
man-db
manpages
manpages-dev
manpages-posix
manpages-posix-dev
gcc-doc
cpp-doc
glibc-doc
python3-doc
bash-doc
